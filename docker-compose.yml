# 本地开发/测试用
# 使用: docker compose up -d

services:
  blog:
    build: .
    image: lyleton/myblog:latest
    container_name: myblog
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
